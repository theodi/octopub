<h2>Add Dataset</h2>

<%= bootstrap_form_tag url: datasets_path, method: :post do |f| %>
  <%= f.text_field "dataset[name]", label: "Dataset name" %>
  <fieldset>
    <h3>Data files</h3>
    <div class="well bg-info">
      <%= f.file_field "files[][file]", label: "File" %>
    </div>
  </fieldset>
  <%= f.submit "Submit" %>
<% end %>
