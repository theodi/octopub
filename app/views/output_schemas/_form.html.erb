<%= form_for(output_schema) do |f| %>
  <% if output_schema.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(output_schema.errors.count, "error") %> prohibited this output_schema from being saved:</h2>

      <ul>
      <% output_schema.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
