require 'rails_helper'

describe CheckRepositoryAccess do
  
  context "when the user has access" do
  
    it "should make no changes" do
      pending    
      expect(false).to eq true
    end
    
  end
  
  context "when the user doesn't have access" do
  
    it "should add user as a collaborator" do
      pending    
      expect(false).to eq true
    end
    
  end

end
